#### Do not edit this file, it is generated
#### Edit the files in /home/akosmin/.config/i3/conf.d
#### Then run /home/akosmin/.config/i3/i3_config reload|restart

set $mod Mod1
set $left h
set $down j
set $up k
set $right l
set $monitor-left DisplayPort-0
set $monitor-right DisplayPort-1
set $ws1 1
set $ws2 2
set $ws3 3
set $ws4 4
# set $term alacritty
set $term termite
# follow mouse for window focus
focus_follows_mouse yes

floating_modifier $mod

bindsym $mod+Return exec --no-startup-id $term
bindsym $mod+space exec --no-startup-id st
# vim: set ft=conf :
set_from_resource $black i3wm.color0 #000000
set_from_resource $black_h i3wm.color8 #000000
set_from_resource $red  i3wm.color1  #000000
set_from_resource $red_h  i3wm.color9  #000000
set_from_resource $green i3wm.color2 #000000
set_from_resource $green_h i3wm.color10 #000000
set_from_resource $yellow  i3wm.color3  #000000
set_from_resource $yellow_h  i3wm.color11  #000000
set_from_resource $blue  i3wm.color4  #000000
set_from_resource $blue_h  i3wm.color12  #000000
set_from_resource $purple  i3wm.color5  #000000
set_from_resource $purple_h  i3wm.color13  #000000
set_from_resource $teal  i3wm.color6  #000000
set_from_resource $teal_h  i3wm.color14  #000000
set_from_resource $white i3wm.color7 #ffffff
set_from_resource $white_h i3wm.color15 #ffffff

# class                 border  backgrd text    indicator child_border
client.focused          $black  $black  $white  $yellow   $black_h
client.focused_inactive $black  $black  $white  $black    $black
client.unfocused        $black  $black  $white  $black    $black
client.urgent           $purple $black  $white  $purple  $purple
client.placeholder      $black  $black  $white  $black    $black

#client.background $black

# green gruvbox
# class                 border|backgr|text|indicator|child_border
#client.focused          $green $green $darkgray $purple $darkgray
#client.focused_inactive $darkgray $darkgray $yellow $purple $darkgray
#client.unfocused        $darkgray $darkgray $yellow $purple $darkgray
#client.urgent           $red $red $white $red $red

# blue gruvbox
# class                 border|backgr|text|indicator|child_border
#client.focused          $blue $blue $darkgray $purple $darkgray
#client.focused_inactive $darkgray $darkgray $yellow $purple $darkgray
#client.unfocused        $darkgray $darkgray $yellow $purple $darkgray
#client.urgent           $red $red $white $red $red
new_window pixel 3
new_float normal
floating_modifier $mod

# Window kill command
bindsym $mod+x kill

# change focus
bindsym $mod+$left focus left
bindsym $mod+$down focus down
bindsym $mod+$up focus up
bindsym $mod+$right focus right

# move focused window
bindsym $mod+Shift+$left move left
bindsym $mod+Shift+$down move down
bindsym $mod+Shift+$up move up
bindsym $mod+Shift+$right move right

# toggle fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# split orientation
bindsym $mod+s split toggle

# toggle between splitv and splith layouts
bindsym $mod+Shift+s layout toggle splitv splith

# toggle tiling vs. floating
# bindsym $mod+Tab floating toggle

# change focus between tiling / floating windows
bindsym $mod+Shift+Tab focus mode_toggle

# focus the parent container
#bindsym $mod+a focus parent

# move the currently focused window to the scratchpad
#bindsym $mod+Shift+minus move scratchpad

# Show the next scratchpad window or hide the focused scratchpad window.
# If there are multiple scratchpad windows, this command cycles through them.
#bindsym $mod+minus scratchpad show

# Open specific applications in floating mode
for_window [class="Pavucontrol"] floating enable
for_window [class="qt5ct"] floating enable sticky enable border normal
for_window [class="(?i)virtualbox"] floating enable border normal
for_window [class="Xfburn"] floating enable
for_window [class="Xephyr"] floating disable

# switch to workspace with urgent window automatically
for_window [urgent=latest] focus

gaps inner 15
# gaps outer 5
smart_gaps on

# these are not working ATM
#bindsym $mod+g gaps inner current set 0; gaps outer current set 0
#bindsym $mod+Shift+g gaps inner current set 15; gaps outer current set 15
# vim: set ft=conf :

set $mode_system System: (l)ock, (e)xit, switch_(u)ser, (s)uspend, (h)ibernate, (r)eboot, (Shift+s)hutdown
set $mode_i3 i3: (r)eload, (R)estart, (e)xit, (t)ermite config
set $mode_gaps Gaps: (o) outer, (i) inner
set $mode_gaps_outer Outer Gaps: +|-|0 (local), Shift + +|-|0 (global)
set $mode_gaps_inner Inner Gaps: +|-|0 (local), Shift + +|-|0 (global)

bindsym $mod+r mode "$mode_resize"
bindsym $mod+i mode "$mode_i3"
bindsym $mod+Escape mode "$mode_system"
bindsym $mod+Shift+g mode "$$mode_gaps"

mode "$mode_system" {
  bindsym l exec --no-startup-id i3exit lock, mode "default"
  bindsym s exec --no-startup-id i3exit suspend, mode "default"
  bindsym u exec --no-startup-id i3exit switch_user, mode "default"
  bindsym e exec --no-startup-id i3exit logout, mode "default"
  bindsym h exec --no-startup-id i3exit hibernate, mode "default"
  bindsym r exec --no-startup-id i3exit reboot, mode "default"
  bindsym Shift+s exec --no-startup-id i3exit shutdown, mode "default"
  bindsym Return mode "default"
  bindsym Escape mode "default"
}

mode "$mode_i3" {
  bindsym r exec --no-startup-id ~/.config/i3/i3_config reload
  bindsym Shift+r exec --no-startup-id ~/.config/i3/i3_config restart
  bindsym e exec --no-startup-id "i3-msg exit"
  bindsym Return mode "default"
  bindsym Escape mode "default"
}

mode "$mode_resize" {
  bindsym $down resize grow height 5 px or 5 ppt
  bindsym $up resize shrink height 5 px or 5 ppt
  bindsym $left resize shrink width 5 px or 5 ppt
  bindsym $right resize grow width 5 px or 5 ppt
  bindsym Return mode "default"
  bindsym Escape mode "default"
}

mode "$mode_gaps" {
  bindsym o      mode "$mode_gaps_outer"
  bindsym i      mode "$mode_gaps_inner"
  bindsym Return mode "default"
  bindsym Escape mode "default"
}

mode "$mode_gaps_inner" {
  bindsym plus  gaps inner current plus 5
  bindsym minus gaps inner current minus 5
  bindsym 0     gaps inner current set 0

  bindsym Shift+plus  gaps inner all plus 5
  bindsym Shift+minus gaps inner all minus 5
  bindsym Shift+0     gaps inner all set 0

  bindsym Return mode "default"
  bindsym Escape mode "default"
}

mode "$mode_gaps_outer" {
  bindsym plus  gaps outer current plus 5
  bindsym minus gaps outer current minus 5
  bindsym 0     gaps outer current set 0

  bindsym Shift+plus  gaps outer all plus 5
  bindsym Shift+minus gaps outer all minus 5
  bindsym Shift+0     gaps outer all set 0

  bindsym Return mode "default"
  bindsym Escape mode "default"
}

workspace $ws1 output $monitor-left
workspace $ws2 output $monitor-right
workspace $ws3 output $monitor-left
workspace $ws4 output $monitor-right

# workspace back and forth
workspace_auto_back_and_forth yes
bindsym Control+k workspace back_and_forth
bindsym $mod+Tab workspace back_and_forth

# next/prev workspace
bindsym Control+l workspace next
bindsym Control+Left workspace next
bindsym Control+h workspace prev
bindsym Control+Right workspace prev

# move to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4

# Send focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4

# Move to workspace and bring focused container
bindsym $mod+Control+1 move container to workspace $ws1; workspace $ws1
bindsym $mod+Control+2 move container to workspace $ws2; workspace $ws2
bindsym $mod+Control+3 move container to workspace $ws3; workspace $ws3
bindsym $mod+Control+4 move container to workspace $ws4; workspace $ws4

# move workspace between screens
bindsym $mod+bracketleft move workspace to output left
bindsym $mod+bracketright move workspace to output right
bar {
  status_command i3blocks
  position top
  mode dock
  modifier None
  font pango:Hack,FontAwesome 14
  output primary
  tray_output primary
  colors {
    separator $white
    background $black
    statusline $white
    focused_workspace $black $green $black
    active_workspace  $black  $black  $white
    inactive_workspace  $black  $black  $white
    urgent_workspace  $black  $purple $black
  }
  # Take a screenshot by right clicking on the bar
  # bindsym --release button3 exec --no-startup-id import /tmp/latest-screenshot.png
}

bar {
  status_command i3blocks -c $HOME/.config/i3blocks/config2
  position top
  modifier None
  font pango:FontAwesome 14
  output DisplayPort-1
  binding_mode_indicator no
  colors {
    separator $white
    background $black
    statusline $white
    focused_workspace $black $green $black
    active_workspace  $black  $black  $white
    inactive_workspace  $black  $black  $white
    urgent_workspace  $black  $purple  $black
  }
}
# Firefox
assign [class="^Firefox$"] $ws2
for_window [title="^Twitch - Mozilla Firefox$"] move workspace $ws1

# Twitch chat
#for_window [title="^mtf8_music - Twitch - Mozilla Firefox$"] move workspace $ws1

# jupyter notebook
#for_window [title="^Home Page - Select or create a notebook - Mozilla Firefox$"] move workspace $ws1

# qtractor in a single window
for_window [class="(?i)Qtractor" instance="(?i)Qtractor"] floating disable
assign [class="(?i)Qtractor" instance="(?i)Qtractor"] $ws4

# konfyt
# assign [class="konfyt"] $ws3

# key-mon
for_window [instance="key-mon"] floating enable

# rofi
#bindsym $mod+d exec --no-startup-id "rofi -modi drun,run -show drun"
bindsym $mod+d exec --no-startup-id rofi -show run

# zrythm
assign [class="^Zrythm$"] $ws4

# OBS
assign [class="^obs$"] $ws1

# VirtualBox
assign [class="^VirtualBox Manager$"] $ws3
# on every restart
exec_always --no-startup-id xrdb -merge ~/.Xresources
